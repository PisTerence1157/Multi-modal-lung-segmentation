# Attention U-Net configuration
# Inherits from base.yaml - only model-specific overrides here

model:
  name: "attention_unet"
  in_channels: 1
  out_channels: 1
  features: [64, 128, 256, 512]
  dropout: 0.15
  bilinear: true
